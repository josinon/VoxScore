apiVersion: v1
kind: Service
metadata:
  name: voxscore-frontend
  labels:
    app.kubernetes.io/name: voxscore-frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: voxscore-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
